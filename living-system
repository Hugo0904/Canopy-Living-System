#!/usr/bin/env python3
from __future__ import annotations

import os
import sys
from pathlib import Path


ENTRYPOINT = Path(__file__).resolve().with_name("observatory")
os.execv(sys.executable, [sys.executable, str(ENTRYPOINT), *sys.argv[1:]])
